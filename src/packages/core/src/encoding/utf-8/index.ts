/**
 * Converts plaintext to UTF-8 string.
 * @param text - The text to convert.
 * @param compact - When `true`, encodes characters as `'\u{xx}'` instead of `'\u{00xx}'`.
 * @returns The string encoded in UTF-8.
 * @public
 */
export function encodeUTF8(text: string, compact?: boolean): string {
  return ''
}

/**
 * Converts UTF-8 string to plaintext.
 * @param text - The text to convert.
 * @returns The string decoded from UTF-8.
 * @public
 */
export function decodeUTF8(text: string): string {
  return ''
}
