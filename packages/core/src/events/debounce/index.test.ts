import { createDebouncedCallback, createDebouncedPromise } from '.'

test(createDebouncedCallback.name, (): void => {
  // TODO: createDebouncedCallback()
})

test(createDebouncedPromise.name, (): void => {
  // TODO: createDebouncedCallback()
})
