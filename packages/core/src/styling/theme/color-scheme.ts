/**
 * @public
 */
export enum ColorScheme {
  dark = 'dark',
  light = 'light',
}

/**
 * @public
 */
export enum LenientColorScheme {
  dark = ColorScheme.dark,
  light = ColorScheme.light,
  auto = 'auto',
}
